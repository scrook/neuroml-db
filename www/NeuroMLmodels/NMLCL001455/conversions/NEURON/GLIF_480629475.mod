TITLE Mod file for component: Component(id=GLIF_480629475 type=glifAscCell)

COMMENT

    This NEURON file has been generated by org.neuroml.export (see https://github.com/NeuroML/org.neuroml.export)
         org.neuroml.export  v1.5.3
         org.neuroml.model   v1.5.3
         jLEMS               v0.9.9.0

ENDCOMMENT

NEURON {
    POINT_PROCESS GLIF_480629475
    
    
    NONSPECIFIC_CURRENT i                    : To ensure v of section follows v_I
    RANGE leakConductance                   : parameter
    RANGE leakReversal                      : parameter
    RANGE tau1                              : parameter
    RANGE tau2                              : parameter
    RANGE amp1                              : parameter
    RANGE amp2                              : parameter
    RANGE thresh                            : parameter
    RANGE reset                             : parameter
    RANGE C                                 : parameter
    
    RANGE iSyn                              : exposure
    
    RANGE iMemb                             : exposure
    
    RANGE copy_v                           : copy of v on section
    
}

UNITS {
    
    (nA) = (nanoamp)
    (uA) = (microamp)
    (mA) = (milliamp)
    (A) = (amp)
    (mV) = (millivolt)
    (mS) = (millisiemens)
    (uS) = (microsiemens)
    (molar) = (1/liter)
    (kHz) = (kilohertz)
    (mM) = (millimolar)
    (um) = (micrometer)
    (umol) = (micromole)
    (S) = (siemens)
    
}

PARAMETER {
    
    leakConductance = 0.002283368 (uS)
    leakReversal = 0 (mV)
    tau1 = 33.333336 (ms)
    tau2 = 3.3333335 (ms)
    amp1 = -0.03847001 (nA)
    amp2 = 0.20589893 (nA)
    thresh = 16.535833 (mV)
    reset = 0 (mV)
    C = 4.9467773E-5 (microfarads)
}

ASSIGNED {
    v (mV)
    i (mA/cm2)
    
    copy_v (mV)
    
    v_I (nA) 
    
    iSyn (nA)                              : derived variable
    
    iMemb (nA)                             : derived variable
    rate_v (mV/ms)
    rate_asc1 (nA/ms)
    rate_asc2 (nA/ms)
    
}

STATE {
    asc1 (nA) 
    asc2 (nA) 
    
}

INITIAL {
    rates()
    rates() ? To ensure correct initialisation.
    
    net_send(0, 1) : go to NET_RECEIVE block, flag 1, for initial state
    
}

BREAKPOINT {
    
    SOLVE states METHOD cnexp
    
    
    copy_v = v
    i = v_I * C
}

NET_RECEIVE(flag) {
    
    if (flag == 1) { : Setting watch for top level OnCondition...
        WATCH (v >  thresh) 1000
    }
    if (flag == 1000) {
    
        asc1 = asc1  +  amp1
    
        asc2 = asc2  +  amp2
    
        v = reset
    
        v_I = 0 : Setting rate of change of v to 0
    }
    if (flag == 1) { : Set initial states
    
        v = leakReversal
    }
    
}

DERIVATIVE states {
    rates()
    asc1' = rate_asc1 
    asc2' = rate_asc2 
    
}

PROCEDURE rates() {
    
    ? DerivedVariable is based on path: synapses[*]/i, on: Component(id=GLIF_480629475 type=glifAscCell), from synapses; null
    iSyn = 0 ? Was: synapses[*]_i but insertion of currents from external attachments not yet supported ? path based, prefix = 
    
    iMemb = leakConductance  * (  leakReversal   - v) +  asc1  +  asc2  +  iSyn ? evaluable
    rate_v = iMemb  /  C ? Note units of all quantities used here need to be consistent!
    rate_asc1 = -1 *  asc1  /  tau1 ? Note units of all quantities used here need to be consistent!
    rate_asc2 = -1 *  asc2  /  tau2 ? Note units of all quantities used here need to be consistent!
    
    v_I = -1 * rate_v
     
    
}

